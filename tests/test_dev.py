
# arm_dim = [[(size[0], size[1]) for size in arm] for arm in arm_dims]
# starfish2 = creatures.DevStarfish(space, arm_dim, (1000, 900), 50, node_mass=NODE_MASS,
#                                  materials=materials, muscle_n_groups=MUSCLE_N_GROUP,
#                                  section_cls=springs.CentralBoneSection,
#                                  height_dev_factor=0.5)
# space.add_entity(starfish)

# TODO: add test like that.
# arm_dim_half = [[(size[0]/2, size[1]) for size in arm] for arm in arm_dims]
# starfish2 = creatures.DevStarfish(space, arm_dim_half, (1000, 900), 50, node_mass=NODE_MASS,
#                                  materials=materials, muscle_n_groups=MUSCLE_N_GROUP,
#                                  section_cls=springs.CentralBoneSection,
#                                  height_dev_factor=1.0)
# space.add_entity(starfish2)
# TODO: add test like that.
